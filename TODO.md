# TODO

Сделано (см. полный список приоритетов в аудите проекта):
- [x] `core/detect.py` — находит табличку, режет один лучший кроп в памяти
- [x] `core/decode.py` — распознавание цифр через `digit_detector`
      (вместо EasyOCR) + геометрический отбор строки цифр и нормализация
      по длине
- [x] `core/eval.py` — точность против имён файлов в `test/`
- [x] `gui.py` переведён на `core/detect.py` + `core/decode.py`, связь
      кроп↔исходное фото больше не теряется; лимита на число файлов нет
- [x] Обновление интерфейса из фонового потока через очередь
      (`queue.Queue` + `root.after`), а не `root.update()` напрямую
- [x] Манифест переименования (`manifest.json`) и кнопка "Undo last run"
      (переименование — это копия в `renamed/`, оригиналы не трогаются)
- [x] Витерби по EXIF-порядку съёмки для исправления оставшихся ошибок
      (`core/sequence.py`)
- [x] Честный train/val split по идентичности таблички
      (`prepare_split.py`) и дообучение детектора на нём
- [x] ONNX + onnxruntime вместо torch/ultralytics в рантайме приложения
      (`core/onnx_backend.py`) — обучение по-прежнему на ultralytics
      (`requirements-train.txt`), сам `gui.py` его больше не тянет
- [x] PyInstaller-сборка (`PlateOCR.spec`) для офлайн-раздачи без Python

Дальше:
- [ ] Экран ручной проверки распознанных номеров перед переименованием
