# TODO

Сделано:
- [x] `core/detect.py` — находит табличку, режет один лучший кроп в памяти
- [x] `core/decode.py` — распознавание цифр через `digit_detector.pt`
      (вместо EasyOCR) + геометрический отбор строки цифр и нормализация
      по длине
- [x] `core/eval.py` — точность против имён файлов в `test/`
- [x] `gui.py` переведён на `core/detect.py` + `core/decode.py`, связь
      кроп↔исходное фото больше не теряется

Дальше (см. полный список приоритетов в аудите проекта):
- [ ] Убрать лимит в 100 файлов, потоковая обработка больших папок
- [ ] Обновление интерфейса из фонового потока через очередь, а не
      `root.update()` напрямую — сейчас источник зависаний
- [ ] Манифест переименования и кнопка "Отменить" (сейчас `shutil.move`
      необратим)
- [ ] Витерби по EXIF-порядку съёмки для исправления оставшихся ошибок
- [ ] Экран ручной проверки распознанных номеров перед переименованием
- [ ] Честный train/val split по сессиям съёмки и дообучение детектора
- [ ] ONNX + PyInstaller для офлайн-поставки
